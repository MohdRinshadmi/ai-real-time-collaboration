apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: collab
spec:
  type: ClusterIP
  selector: { app: api }
  ports:
    - { name: http, port: 80, targetPort: 4000 }
    - { name: metrics, port: 9464, targetPort: 9464 }
